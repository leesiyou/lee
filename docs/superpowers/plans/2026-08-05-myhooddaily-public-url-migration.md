# Myhood Daily 公网地址迁移实施计划

1. 只读确认节点小宝映射、额度和博客内网目标。
2. 将博客公网子域名改为 `myhooddaily`，保留其他项目映射。
3. 先添加静态 H5 新地址契约测试并确认旧实现失败。
4. 更新 `PUBLIC_BASE_URL` 默认值、街舞 H5 元数据与复制链接、公众号素材及部署报告。
5. 运行定向测试、全量测试、类型检查、构建和差异检查。
6. 提交推送、构建新镜像并部署到飞牛 NAS。
7. 验证公网 HTTPS、H5 资源、容器健康和桌面发布箱，最后复制新链接。
